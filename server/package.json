{
  "name": "redline-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "REDLINE server — Socket.IO authoritative game server. Routes inputs, broadcasts world snapshots, schedules meteor showers, relays combat events.",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cannon": "^0.6.2",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
